# Member (frontend)

Public reader views for `/site/member/*` plus a thin console directory at `/dash/members` (`members.index`).

Login, register, account, verified. Uses `auth:member` token in the member store. Console list is operators-only (`view members`).
