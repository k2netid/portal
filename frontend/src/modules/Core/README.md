# Core (frontend)

Unified console SPA: System, Infra, Security views under `frontend/src/modules/Core/`.

Kernel settings Identity tab owns `general`. Media tab is storage/S3/FTP (the Media pack is the file library).
