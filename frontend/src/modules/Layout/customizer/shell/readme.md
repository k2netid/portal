# Customizer shell

Admin UI for editing theme settings (`themes/:slug/customizer`).

| File | Role |
|------|------|
| `ThemeCustomizerView.vue` | Full customizer organization (design / bindings / advanced) |

Legacy import path: `views/themes/ThemeCustomizer.vue` re-exports this component.
