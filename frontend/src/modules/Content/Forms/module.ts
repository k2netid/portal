/**
 * Content Studio submodule barrel — routes/navigation register via `Studio/module.ts`.
 * Re-exported here for direct imports (Layout, Forms, Library).
 */
export { ContentStudioModule as FormsModule } from '../Studio/module';
export { default } from '../Studio/module';
