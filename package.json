{
    "name": "jejakawan",
    "version": "1.0.0-beta.2",
    "private": true,
    "engines": {
        "node": ">=22.12.0"
    },
    "scripts": {
        "dev": "bash scripts/use-node22.sh npm --prefix frontend run dev",
        "build": "bash scripts/use-node22.sh npm --prefix frontend run build",
        "sync": "npm run deploy:assets:sync",
        "deploy": "npm run deploy:assets:full",
        "deploy:assets": "npm run deploy:assets:full",
        "deploy:assets:full": "bash scripts/sync-frontend-assets-to-backend.sh",
        "agent:verify": "npm run quality:frontend && npm run quality:backend && npm run check:client-hardcoding && npm run docs:links && npm run versions:check && npm run modules:versions:check",
        "agent:verify:quick": "npm run quality:frontend",
        "check:client-hardcoding": "node scripts/check-zero-client-hardcoding.mjs",
        "docs:links": "node scripts/check-docs-links.mjs",
        "versions:check": "node scripts/check-versions.mjs",
        "modules:versions:check": "node scripts/check-module-versions.mjs",
        "docs:sync-downstream": "bash scripts/sync-docs-guides-reference-downstream.sh",
        "docs:openapi": "bash scripts/export-openapi.sh",
        "test:backend": "cd backend && php artisan test",
        "test:backend:coverage": "cd backend && php artisan test --coverage",
        "test:e2e:list": "npm --prefix frontend run test:e2e:list",
        "test:e2e:smoke": "npm --prefix frontend run test:e2e:smoke",
        "test:e2e:smoke:docker": "bash scripts/e2e-docker.sh",
        "test:e2e:auth": "npm --prefix frontend run test:e2e:auth",
        "format:backend": "cd backend && ./vendor/bin/pint",
        "quality:frontend": "bash scripts/use-node22.sh npm --prefix frontend run quality:frontend",
        "quality:backend": "cd backend && ./vendor/bin/pint --test && composer run quality",
        "release": "bash scripts/bundle.sh",
        "install:server": "sudo bash scripts/install.sh",
        "deploy:assets:sync": "SYNC_ONLY=1 bash scripts/sync-frontend-assets-to-backend.sh"
    },
    "x-agent-guidelines": {
        "purpose": "Standard instructions for AI/coding agents when creating or updating this project.",
        "scope": "Jejakawan Core Engine; repo ja-core_engine (Core System, Security, Infra, Data Model Studio, IAM, Engine Architecture).",
        "when_creating_or_updating_project": [
            "Read docs/WORKFLOW.md and docs/DOCUMENTATION.md first, then docs/AGENT_START_HERE.md, docs/architectural-status.md, docs/product/downstream-apps-and-licensing.md, and docs/architecture/*.md listed in docs/README.md.",
            "For scale M+ work: create/update a brief under docs/work/ from docs/templates/task-brief.md (audit → plan → implement → verify → record).",
            "Use branch-based workflow; avoid direct edits on main unless explicitly requested.",
            "Prefer package managers for dependency changes (npm/composer), avoid manual lockfile edits.",
            "Keep changes minimal and focused; avoid unrelated refactors.",
            "Do not commit secrets or environment-specific credentials.",
            "Update dual-layer changelogs per docs/guides/update-changelog.md when behavior or system docs change.",
            "After edits, run npm run agent:verify before proposing merge."
        ],
        "required_validation_before_pr": [
            "npm run quality:frontend",
            "npm run quality:backend"
        ],
        "optional_validation": [
            "npm run quality:all",
            "npm run test:e2e:list"
        ],
        "handover_checklist": [
            "Summarize risky files touched and behavior changes.",
            "Include test evidence (commands + pass/fail).",
            "Ensure working tree is clean before final handover."
        ]
    }
}
