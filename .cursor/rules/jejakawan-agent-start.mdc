---
description: Jejakawan Core Engine — agent identity & start pointers
alwaysApply: true
---

# Jejakawan Core Engine (`ja-core_engine`)

This repo is the **headless kernel + unified admin console**, not `ja-cms` and not `ja-control-plane`.

## Canonical facts
- **Branch:** `main` only (CMS `develop` line retired Aug 2026).
- **Kernel modules:** `Modules/Core` → System, Infra, Security. Optional first-party extension: **JA-Mail** (`Modules/Mail`).
- **Stack:** Laravel 12 modular API · Vue 3 unified console SPA · PostgreSQL/SQLite · Redis.
- **Downstream apps** (CMS, portals, SaaS) = **separate forks**, not long-lived branches here.

## Read first
1. [docs/AGENT_START_HERE.md](../../docs/AGENT_START_HERE.md)
2. [docs/architectural-status.md](../../docs/architectural-status.md)
3. [AGENTS.md](../../AGENTS.md)

## Do / don't
- Do not call this product `ja-cms` / `JA-CMS` in new user-facing copy.
- Do not re-add Content/Layout/Publishing/themes to `main` unless explicitly building a downstream product line.
- Prefer minimal diffs; run `npm run agent:verify` (or CI gates) before calling work done.
- Never commit secrets / `.env`.
- Keep per-module `README.md` + `CHANGELOG.md` updated (see `.cursor/rules/module-documentation.mdc`).
